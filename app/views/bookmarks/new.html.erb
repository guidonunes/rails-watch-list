<h1>Add a Movie for <%= @list.name %></h1>

<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.input :movie_id, collection: @movies%>
  <%= f.submit 'Add', class: 'btn btn-primary' %>
<% end %>

<%= link_to "Back", list_path(@list) %>
